[Authorize]
public IActionResult Index()
{
    if (User.IsInRole("Seller"))
    {
        return RedirectToAction("Dashboard", "Seller");
    }
    return RedirectToAction("Privacy"); // or any other default page
}